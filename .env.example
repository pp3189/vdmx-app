# VDMX Risk Intelligence - Environment Variables
# Copy this file to .env and fill in your values

# === STRIPE CONFIGURATION ===
# Get these from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_YOUR_KEY_HERE
STRIPE_WEBHOOK_SECRET=whsec_YOUR_WEBHOOK_SECRET_HERE

# === DATABASE ===
# PostgreSQL connection string (optional - uses local JSON if not set)
DATABASE_URL=postgresql://user:password@host:port/database

# === SECURITY ===
# Admin token for accessing /api/admin/* endpoints
# Generate a secure token: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_SECRET_TOKEN=your-secure-admin-token-here

# === URLS ===
# Frontend client URL
CLIENT_URL=http://localhost:3000

# Server URL for file upload links (production)
SERVER_URL=https://vdmx-app-production.up.railway.app

# === ENVIRONMENT ===
# Set to 'production' to enable security features
NODE_ENV=development
